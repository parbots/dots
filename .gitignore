# TUI build artifacts
tui/dots
dots

# OS files
.DS_Store
Thumbs.db

# Worktrees (isolated workspaces, never tracked)
.worktrees/

# Local Claude Code state (session locks, machine-specific settings — never shared)
.claude/
